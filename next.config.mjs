/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.themealdb.com'], // Add the domain of the API where images are hosted
  },
  reactStrictMode: true,
};

export default nextConfig;
